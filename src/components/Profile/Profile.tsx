import React from 'react';
import Styled from './styled'

const Profile = () => {
    return (
        <Styled.ProfileWrapper>
            profile
        </Styled.ProfileWrapper>
    );
};

export default Profile;